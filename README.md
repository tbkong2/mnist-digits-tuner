This project builds and evaluates a Convolutional Neural Network (CNN) for handwritten digit classification using the MNIST dataset. It performs hyperparameter tuning with Stratified K-Fold cross-validation to determine the optimal number of convolutional filters and learning rate. After identifying the best configuration, the final model is trained on the full dataset and tested for performance accuracy.
